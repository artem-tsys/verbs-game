export const modalNameSelector = (state) => state.common.modal.name;
export const modalDataSelector = (state) => state.common.modal.data;
export const languageSelector = (state) => state.common.language;
